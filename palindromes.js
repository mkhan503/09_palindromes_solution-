const palindromes = function (expression) {
    //split the expression into an array. Including characters and white space
    let arr1 = expression.split("");

    //remove whitespace and characters from string

    //create a new array reversed

    //use array method to compare the two arrays. 
    

};

module.exports = palindromes;
